#include <SDL2/SDL.h>
#include "Renderer.h"

int main (int argc, char *argv[]){
    
    SDL_Init(SDL_INIT_EVERYTHING);
    Renderer Engine_Renderer;

    

    SDL_Quit();

    return 0;
}